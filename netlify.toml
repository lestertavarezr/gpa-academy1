[build]
  base = "guardia-quirofano"
  command = "npm ci && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
